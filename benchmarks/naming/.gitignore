# Generated by score.py: a per-run HTML view of the same data results.json
# already records. Regenerated on every scoring run, so tracking it would
# add a large diff per run with no information the JSON lacks.
report.html
report-*.html

# Scratch prediction files. The committed predictions_*.json are the
# recorded runs; anything else is a working file from a verification pass.
predictions_check.json
