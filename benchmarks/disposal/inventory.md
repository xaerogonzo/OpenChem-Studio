# Every hand-written Qt disposal sequence under `tests/`

Generated by `tools/disposal_inventory.py`. Do not hand-edit;
run the script.

**THE PRE-CONSOLIDATION STATE IS THE EVIDENCE, AND IT IS ONE
COMMAND AWAY:** `git show dba03eb:benchmarks/disposal/inventory.md`
-- 64 sequences, 8 distinct, 46 files, recorded before a single
site was touched. A green suite could not have been that evidence:
the crash this work is about is roughly 50/50 on a byte-identical
tree, so the suite cannot detect a change in it in either
direction.

What is left below is what SHOULD be left. The canonical
`setParent(None) -> deleteLater() -> flush` appears exactly once,
in `conftest.dispose`, and
`test_no_test_file_derives_the_disposal_recipe_for_itself` is what
keeps it that way. Everything else here is a sequence that is not
that recipe and was deliberately left alone.

- sequences: **7**
- distinct sequences: **5**
- files: **3**

## 2x  close()  ->  sendPostedEvents(<subject>, QEvent.Type.DeferredDelete)

- `tests/test_pop_out_host.py:160` (`window`)
- `tests/test_pop_out_host.py:203` (`window`)

## 2x  sendPostedEvents(<subject>, QEvent.Type.DeferredDelete)

- `tests/conftest.py:115` (`obj`)
- `tests/conftest.py:200` (`widget`)

## 1x  deleteLater()  ->  sendPostedEvents(<subject>, QEvent.Type.DeferredDelete)

- `tests/test_property_panel_long_values.py:489` (`parent`)

## 1x  stop()  ->  deleteLater()  ->  sendPostedEvents(<subject>, QEvent.Type.DeferredDelete)

- `tests/conftest.py:296` (`view`)

## 1x  stop()  ->  setParent(None)  ->  deleteLater()  ->  sendPostedEvents(<subject>, QEvent.Type.DeferredDelete)

- `tests/conftest.py:418` (`view`)

