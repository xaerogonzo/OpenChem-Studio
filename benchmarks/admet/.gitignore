# Regenerated by fetch_tdc.py / predict.py / score.py. Not committed: the
# splits are TDC's data, the predictions are ~15 MB, and both are one
# command away. README.md records the scores that matter.
tdc_data/
predictions_*.csv
scores.csv
tdcenv/
