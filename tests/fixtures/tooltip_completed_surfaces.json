{
  "_comment": "Surfaces whose every documentable control carries a help contract. THE OPPOSITE OF tooltip_migration_debt.json: that set may only shrink, this one may only GROW. Without it, removing a contract from a finished surface is invisible -- the coverage guard cannot fail on `missing`, because 84 controls still are. Add a surface here the moment it reaches zero. See tests/test_tooltip_coverage.py.",
  "by_kind": [
    "action"
  ],
  "by_instance_path_fragment": [
    "QuantumChemistryPanel"
  ]
}
